--- a/src/server.cc	2018-03-20 02:41:19.000000000 +0100
+++ b/src/server.cc	2018-04-25 10:52:07.841237040 +0200
@@ -155,13 +155,11 @@
         for (int i = 0; i < arr->size(); i++) {
             tmp = arr->get(i);
             if (string_ok(tmp)) {
-                log_info("(NOT) Adding HTTP header \"%s\"\n", tmp.c_str());
-                // FIXME upstream upnp
-                //ret = UpnpAddCustomHTTPHeader(tmp.c_str());
-                //if (ret != UPNP_E_SUCCESS)
-                //{
-                //    throw _UpnpException(ret, _("upnp_init: UpnpAddCustomHTTPHeader failed"));
-                //}
+                ret = UpnpAddCustomHTTPHeader(tmp.c_str());
+                if (ret != UPNP_E_SUCCESS)
+                {
+                    throw _UpnpException(ret, _("upnp_init: UpnpAddCustomHTTPHeader failed"));
+                }
             }
         }
     }
